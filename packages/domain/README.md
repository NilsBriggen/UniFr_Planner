# Domain

Dependency-free Python modules live in `src/unifr_domain`. HTTP handlers, persistence,
clock reads and source fetching belong in adapters. Later tasks add deterministic
planning and requirement logic here. Tests belong in `tests/`.

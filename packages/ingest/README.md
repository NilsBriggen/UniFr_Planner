# Ingestion

Source fetching, parsing and catalogue publishing live in `src/unifr_ingest`.
This foundation does not fetch or publish any university data. Later tasks add
the worker and its fixtures. Tests belong in `tests/`.
